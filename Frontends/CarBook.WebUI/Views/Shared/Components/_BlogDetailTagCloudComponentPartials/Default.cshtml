@model List<ResultTagCloudByBlogIdDtos>
<div class="sidebar -box ftco-animate">
    <h3>Tag Cloud</h3>
       @foreach (var item in Model)
    {
        <div class="tagcloud">
            <a href="#" class="tag-cloud-link">@item.TagCloudTitle</a>

        </div>
    }
</div>
